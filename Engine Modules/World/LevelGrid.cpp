#include "LevelGrid.hpp"

BaseLevelPtrGrid levelGrid = BaseLevelPtrGrid(0);

std::vector<LevelPosition> levelsActiveVector = std::vector<LevelPosition>(0);

LevelPosition levelCur = LevelPosition(0, 0, 0);


